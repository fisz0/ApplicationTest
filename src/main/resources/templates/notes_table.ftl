<table id="notesTable" class="table table-bordered table-responsive">
    <thead>
    <tr>
        <th>Date</th>
        <th>Note</th>
    </tr>
    </thead>
    <tbody>
    <tr>
        <td>
            <button type="button" name="${user.id}" id="userDetailsBtn" class="noteLink">${user.login}</button>
        </td>
        <td>DUPA</td>
    </tr>
    <tr>
        <td>
            <button type="button" name="${user.id}" id="userDetailsBtn" class="noteLink">${user.login}</button>
        </td>
        <td>DUPA</td>
    </tr>
    <tr>
        <td>
            <button type="button" name="${user.id}" id="userDetailsBtn" class="noteLink">${user.login}</button>
        </td>
        <td>DUPA</td>
    </tr>
    <tr>
        <td>
            <button type="button" name="${user.id}" id="userDetailsBtn" class="noteLink">${user.login}</button>
        </td>
        <td>DUPA</td>
    </tr>
    <tr>
        <td>
            <button type="button" name="${user.id}" id="userDetailsBtn" class="noteLink">${user.login}</button>
        </td>
        <td>DUPA</td>
    </tr>
    <tr>
        <td>
            <button type="button" name="${user.id}" id="userDetailsBtn" class="noteLink">${user.login}</button>
        </td>
        <td>DUPA</td>
    </tr>
    <tr>
        <td>
            <button type="button" name="${user.id}" id="userDetailsBtn" class="noteLink">${user.login}</button>
        </td>
        <td>DUPA</td>
    </tr>
    <tr>
        <td>
            <button type="button" name="${user.id}" id="userDetailsBtn" class="noteLink">${user.login}</button>
        </td>
        <td>DUPA</td>
    </tr>
    <tr>
        <td>
            <button type="button" name="${user.id}" id="userDetailsBtn" class="noteLink">${user.login}</button>
        </td>
        <td>DUPA</td>
    </tr>
    <tr>
        <td>
            <button type="button" name="${user.id}" id="userDetailsBtn" class="noteLink">${user.login}</button>
        </td>
        <td>DUPA</td>
    </tr>
    <tr>
        <td>
            <button type="button" name="${user.id}" id="userDetailsBtn" class="noteLink">${user.login}</button>
        </td>
        <td>DUPA</td>
    </tr>
    <tr>
        <td>
            <button type="button" name="${user.id}" id="userDetailsBtn" class="noteLink">${user.login}</button>
        </td>
        <td>DUPA</td>
    </tr>
    <tr>
        <td>
            <button type="button" name="${user.id}" id="userDetailsBtn" class="noteLink">${user.login}</button>
        </td>
        <td>DUPA</td>
    </tr>
    <tr>
        <td>
            <button type="button" name="${user.id}" id="userDetailsBtn" class="linkBtn">${user.login}</button>
        </td>
        <td>DUPA</td>
    </tr>
    <tr>
        <td>
            <button type="button" name="${user.id}" id="userDetailsBtn" class="linkBtn">${user.login}</button>
        </td>
        <td>DUPA</td>
    </tr>
    </tbody>
</table>
